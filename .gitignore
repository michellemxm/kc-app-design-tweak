# runtime state (created by KiroClaw at install/enable time — never publish)
data/
.app_secret
installed.json

# junk
.DS_Store
__pycache__/
node_modules/
*.log
*.bak

# local editor / CLI config — not part of the app
.kiro/
